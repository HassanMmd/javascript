function findCircumference(radius = 4) {
    let circumference = 2 * Math.PI * radius;
    return circumference;
}

function findSurfArea(radius = 4) {
    let surfArea = Math.PI * radius ** 2;
    return surfArea;
}